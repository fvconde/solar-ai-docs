# Execução — S-36 — 39bc1451-73c0-4eef-9f2e-0a374cce736b

- **Página do Notion e ID:** Card S-36 - Imóveis sugeridos persistidos na conversa (`3d63478c-8945-81e9-baa7-c985c4d6ec5a`)
- **Agente / sessão:** Antigravity / Sessão `319a2f23-b740-4e61-9bbc-8154de5f6c0f`
- **Estado / última atividade UTC:** 2026-09-12T20:20:00Z — Concluído (Implementação, testes unitários, testes de integração no Postgres, teste end-to-end e frontend concluídos)
- **Escopo e critérios de aceite consultados:**
  - Card S-36 e `WORKFLOW-AGENTES.md`.
  - Subtarefa 1: Migration `imoveis_sugeridos jsonb null` na tabela `mensagens`. Verificado que o boot aplica a migration sem renomear constraints e `\d mensagens` exibe o tipo `jsonb` nullable.
  - Subtarefa 2: Invariante estrita: `Mensagem.DaLia` sem imóvel grava lista vazia (`'[]'::jsonb`); `Mensagem.DoLead` grava `NULL`. Ambos os estados são distintos e distinguíveis no PostgreSQL. `Mensagem.DaLia` com imóveis grava o snapshot dos cartões (incluindo o motivo contextual daquele turno).
  - Subtarefa 3: DTO `MensagemDaConversa` ganha campo `ImoveisSugeridos` e a projeção de `ConversaRepositorio.HistoricoCompletoAsync` o preenche. Preservação do contrato congelado do `POST /turn` (6 tipos e 37 campos em `ContratoTurno.cs` intocados).
  - Subtarefa 4: `ConversaStore.reconstruir()` no frontend passa a ler os cartões persistidos em vez de devolver `imoveis: []`, associando a intenção do lead para renderizar o preço correto (venda vs aluguel). Remoção do comentário desatualizado e da pendência no `README.md` do front. Validação com Karma e build Angular.
  - Subtarefa 5: Método agregado no repositório (`ObterImoveisSugeridosAsync` e alias `ObterImoveisSugeridosDaConversaAsync`) que retorna os imóveis de toda a conversa, com união deduplicada por `id` preservando a primeira ocorrência para o consumo do S-18 no resumo do corretor.
- **Recursos reservados:**
  - Worktree S-36 em `worktrees/S-36/` para `solar-ai-api`, `solar-ai-front` e `solar-ai-docs` (com `solar-ai` como apoio destacado).
  - Janela de teste integrado no Compose: portas 5432, 8080, 8000 e 4200.
  - Schema, migrations e DbContext do EF Core.
- **Repositórios / caminhos / branches / SHAs base:**
  - `solar-ai-api`: `worktrees/S-36/solar-ai-api` na branch `feature/S-36` (base `origin/develop` SHA `3a71430`)
  - `solar-ai-front`: `worktrees/S-36/solar-ai-front` na branch `feature/S-36` (base `origin/develop` SHA `fad5b61`)
  - `solar-ai-docs`: `worktrees/S-36/solar-ai-docs` na branch `feature/S-36` (base `origin/develop` SHA `ff4c137`)
  - `solar-ai`: `worktrees/S-36/solar-ai` (apoio destacado no commit `af389a6`, sem edição)
- **O que foi alterado:**
  - **solar-ai-api (.NET 10 / SDK 10.0.400):**
    - `src/Solar.Api/Dominio/Mensagem.cs`: Propriedade `ImoveisSugeridos`, factory `DoLead` inicializando com `null`, factory `DaLia` inicializando com lista vazia `[]` quando sem imóvel ou com os itens fornecidos no snapshot.
    - `src/Solar.Api/Dominio/Conversa.cs`: Repasse de `turno.ImoveisSugeridos` para `Mensagem.DaLia` em `RegistrarTurno` e `RegistrarFollowUp`.
    - `src/Solar.Api/Persistencia/SolarDbContext.cs`: Mapeamento da coluna `imoveis_sugeridos` como `jsonb` com conversão JSON segura (null para lead, jsonb para Lia).
    - `src/Solar.Api/Persistencia/Migrations/20260912183807_ImoveisSugeridosNaMensagem.cs`: Migration EF Core com AddColumn/DropColumn simétricos.
    - `src/Solar.Api/Persistencia/Migrations/SolarDbContextModelSnapshot.cs`: Snapshot atualizado com `imoveis_sugeridos`.
    - `src/Solar.Api/Contracts/ContratoConversa.cs`: DTO `MensagemDaConversa` ganha `IReadOnlyList<ImovelSugerido>? ImoveisSugeridos = null`.
    - `src/Solar.Api/Conversas/ConversaRepositorio.cs`: Projeção de `m.ImoveisSugeridos` em `HistoricoCompletoAsync`; métodos `ObterImoveisSugeridosAsync` e `ObterImoveisSugeridosDaConversaAsync` com união deduplicada por `id`.
    - `tests/Solar.Api.Tests/ImoveisSugeridosPostgresTeste.cs`: Testes de integração cobrindo:
      1. Invariante no banco Postgres comprovada via SQL bruto (`imoveis_sugeridos IS NULL` para lead, `'[]'::jsonb` para Lia sem imóvel, e array preenchido com snapshot para Lia com imóveis).
      2. `HistoricoCompletoAsync` preenchendo `MensagemDaConversa.ImoveisSugeridos`.
      3. `ObterImoveisSugeridosAsync` retornando união deduplicada por ID entre múltiplos turnos.
  - **solar-ai-front (Angular 20.3.0 / TypeScript 5.9.2):**
    - `src/app/conversa/contrato.ts`: Interface `MensagemDaConversa` ganha `imoveisSugeridos?: ImovelSugerido[] | null`.
    - `src/app/conversa/conversa-store.ts`:
      - `reconstruir()` lê `mensagem.imoveisSugeridos ?? []` e recebe a intenção do lead (`conversa.perfilLead?.intencao`).
      - Remoção do comentário histórico `"Imoveis nao voltam -- a API nao os persiste"`.
      - `retomarConversa()` repassa `conversa.perfilLead?.intencao` para `reconstruir()`.
      - `verificarNovasMensagens()` preserva `imoveisSugeridos` e intenção ao processar turnos novos via polling.
    - `src/app/conversa/conversa-store.spec.ts`: Testes unitários de retomada validando a reconstrução com 3 cartões, ordem, motivo preservado e intenção de lead para precificação.
    - `README.md`: Remoção da pendência de persistência de imóveis na seção 7.
- **Validações: comando, resultado e ambiente (sem segredos):**
  - **Banco de Dados e Migration:**
    - Migration aplicada via boot da API: `Applying migration '20260912183807_ImoveisSugeridosNaMensagem'.`
    - Verificação de schema no PostgreSQL: `\d mensagens` confirma `imoveis_sugeridos | jsonb | nullable`.
  - **solar-ai-api (.NET):**
    - Comando: `dotnet test`
    - Resultado: `Aprovado: 100, Com falha: 0, Ignorado: 0, Total: 100` (100% verde, incluindo todos os testes de integração Postgres).
  - **solar-ai-front (Angular):**
    - Comando: `npm test -- --watch=false`
    - Resultado: `TOTAL: 45 SUCCESS` (45 testes Karma/Jasmine aprovados, 0 falhas).
    - Comando: `npm run build`
    - Resultado: Build concluído com sucesso em `dist/solar-ai-front`.
  - **Validação Integrada Ponta a Ponta (End-to-End no Compose):**
    - Pilha Compose rodando: `solar-postgres-1`, `solar-api-1`, `solar-agente-1`.
    - `http://localhost:8080/health` respondendo status `up` com verificação do Postgres ativa.
    - Executado cenário com 2 turnos (Turno 1 sem sugestão de imóveis, Turno 2 com 3 imóveis sugeridos com motivos contextuais).
    - `GET http://localhost:8080/conversas/{id}` validado com resposta real:
      - Turno 1 Lead: `imoveisSugeridos: null`
      - Turno 1 Agente: `imoveisSugeridos: []`
      - Turno 2 Lead: `imoveisSugeridos: null`
      - Turno 2 Agente: `imoveisSugeridos: [3 itens completos com Id, Tipo, Bairro, Metragem, PrecoVenda, PrecoAluguel, Motivo]`
- **Pendências ou bloqueios:** Nenhuma pendência técnica.
- **PRs e commits de entrega por repositório:**
  - `solar-ai-api`: commits `b2f390d`, `21641fd`, `cba940c`
  - `solar-ai-front`: commit `e9e3672`
  - `solar-ai-docs`: a commitar com este documento
- **Alterações propostas para ESTADO, arquitetura e vault:**
  - `ESTADO.md`: Registrar conclusão do S-36 e desbloqueio formal do S-18 (resumo do corretor).
  - `ARQUITETURA.md`: Documentar a persistência de snapshots de recomendações na coluna `imoveis_sugeridos` da tabela `mensagens` e a disponibilidade do método de leitura agregada deduplicada.
- **Próximo passo exato:**
  - Abrir os PRs de `feature/S-36` para `develop` em cada repositório alterado (`solar-ai-api`, `solar-ai-front`, `solar-ai-docs`) e atualizar o board do Notion para `Revisão`.
- **Integração: responsável, SHAs em develop e validação conjunta:** A ser preenchido pelo integrador.
