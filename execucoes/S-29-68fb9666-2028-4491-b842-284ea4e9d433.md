# Execução — S-29 — 68fb9666-2028-4491-b842-284ea4e9d433
- Página do Notion e ID: [S-29 · Exclusão de dados do lead, segurança básica e logs](https://app.notion.com/p/S-29-Exclus-o-de-dados-do-lead-seguran-a-b-sica-e-logs-3c43478c89458144ac2ee7d0a7bfc758) (`3c43478c-8945-8144-ac2e-e7d0a7bfc758`)
- Agente / sessão: Antigravity
- Estado / última atividade UTC: Bloqueado / 2026-09-10T17:15:00.000Z
- Escopo e critérios de aceite consultados:
  - Núcleo LGPD (não cortável): endpoint de exclusão que apaga o lead e derruba conversas, mensagens e encaminhamentos em cascata, devolvendo confirmação com escopo e contagem de mensagens excluídas.
  - Verificável ao vivo: antes, o select mostra lead + conversa + mensagens + encaminhamento; depois da chamada, os quatro selects devolvem zero linhas, e a mesma conversa no chat responde 404.
  - Testes reproduzíveis com Postgres real validando cascata, isolamento de leads independentes e concorrência em banco de testes dedicado descartável (`solar_test`), sem tocar no banco de desenvolvimento (`solar`).
  - Higiene de deploy e segurança: autorização obrigatória para qualquer exclusão (lead ou conversa), rate limit por IP (10 req/min para exclusões, 30 req/min para mensagens no chat e `/turn`), CORS restrito e logs estruturados por turno sem PII com medição de latência.
  - Recusa estrita (503 Service Unavailable) quando as chaves de segurança não estiverem configuradas no servidor, sem fallbacks hardcoded.
- Histórico de revisões e remediações:
  - **Revisão 1:** Implementadas travas de concorrência com ordenação por ID (`TravarMultiplasAsync`), autorização de privacidade, rate limiting, logging de turno sem PII e testes Postgres.
  - **Revisão 2 (Bloqueador de Segurança):** Identificado vazamento de senha de desenvolvimento no commit `efabc11` em `tests/Solar.Api.Tests/ExclusaoLeadPostgresTeste.cs`, chave de privacidade pública hardcoded e falhas na autorização/contagem de mensagens em conversas.
  - **Ações corretivas executadas imediatamente:**
    1. **Bloqueio no Notion:** Card marcado como `Bloqueado` e coordenação com S-17 registrada.
    2. **Rotação da senha do Postgres:** Executado `ALTER USER solar WITH PASSWORD` no container `solar-postgres-1`, atualizado arquivo `.env` (gitignored) em `solar-ai-api/` e sincronizado nos worktrees (`S-29`, `S-17`, `S-20`). O container `solar-api-1` foi recriado e reconectado com sucesso (health check `up`). Dados existentes no volume `postgres-data` foram 100% preservados.
    3. **Substituição da chave de privacidade:** Removida a constante pública `ChavePadrao = "solar-privacidade-lgpd-2026"` de `AutorizacaoPrivacidade.cs`. O `appsettings.json` agora possui `ChavePrivacidade: ""` e o valor é carregado exclusivamente de variáveis de ambiente (`SEGURANCA__CHAVEPRIVACIDADE`). Quando a chave não estiver configurada no servidor, a operação é rejeitada com HTTP 503.
    4. **Banco de testes dedicado (`solar_test`):** Criado banco `solar_test` no Postgres. Os testes de integração conectam-se exclusivamente a ele. Foi implementada validação que lança exceção caso a connection string aponte para o banco `solar` de desenvolvimento. Se o banco estiver indisponível, os testes falham explicitamente com `Assert.Fail` em vez de early return vazio. Migrations são aplicadas via `MigrateAsync()` no banco de teste.
    5. **Autorização em conversas e correção de MensagensExcluidas:** `DELETE /conversas/{id}` agora exige autorização para qualquer operação (inclusive exclusão isolada de conversa, evitando deleção anônima). `ExclusaoLeadResultado`, `ExclusaoLeadResponse` e `ExclusaoConversaResponse` agora computam e retornam com exatidão a quantidade de mensagens removidas em cascata.
- Dependências por ID e evidências de conclusão:
  - S-10 (EF Core e Postgres): concluído em `origin/develop` (`af1609a`).
  - S-37 (Encaminhamento ao corretor e contato do lead): concluído em `origin/develop` (`af1609a`).
- Recursos reservados:
  - `DELETE /leads/{id}` e `DELETE /conversas/{id}` em `solar-ai-api`.
  - Rate limiting middleware (`mensagens` e `exclusao`) em `solar-ai-api`.
  - Sem sobreposição com recursos do S-17.
- Repositórios / caminhos / branches / SHAs base:
  - `solar-ai-api`: `worktrees/S-29/solar-ai-api`, branch `feature/S-29`.
  - `solar-ai-docs`: `worktrees/S-29/solar-ai-docs`, branch `feature/S-29`.
- O que foi alterado nesta iteração:
  - `solar-ai-api`:
    - `src/Solar.Api/Contracts/ContratoConversa.cs`: acrescentado `MensagensExcluidas` em `ExclusaoLeadResultado` e `ExclusaoLeadResponse`.
    - `src/Solar.Api/Seguranca/AutorizacaoPrivacidade.cs`: remoção da constante `ChavePadrao`, validação mandatória da chave no servidor (503 caso não configurada).
    - `src/Solar.Api/Controllers/LeadsController.cs`: repasse de `MensagensExcluidas`.
    - `src/Solar.Api/Controllers/ConversasController.cs`: autorização mandatória para todas as exclusões e repasse exato de `MensagensExcluidas` para ambos os escopos.
    - `src/Solar.Api/Conversas/ConversaRepositorio.cs`: contagem e retorno de mensagens removidas em `ExcluirLeadAsync`.
    - `src/Solar.Api/appsettings.json`: chave de privacidade zerada.
    - `.env.example`: adição da variável `SEGURANCA__CHAVEPRIVACIDADE`.
    - `tests/Solar.Api.Tests/ExclusaoLeadTeste.cs`: testes unitários atualizados com chaves mock efêmeras, teste de recusa 503 para servidor sem chave e validação de 401 para `DELETE /conversas/{id}` anônimo.
    - `tests/Solar.Api.Tests/ExclusaoLeadPostgresTeste.cs`: remoção de qualquer segredo, direcionamento exclusivo para `solar_test`, trava contra uso do banco `solar`, fail explícito se indisponível e validação de contagem de mensagens.
- Validações: comando, resultado e ambiente (sem segredos):
  - `dotnet test`: 65 testes aprovados (0 falhas, 0 ignorados) em 1 s no projeto `Solar.Api.Tests`.
  - Testes com PostgreSQL real executados no banco descartável `solar_test` (`localhost:5432`).
  - Verificação de saúde da API: `GET http://localhost:8080/health` retornando `200 OK` com `{"status":"up","checks":{"postgres":{"status":"up"}}}`.
- Plano de limpeza de histórico Git:
  - Identificados os branches e PRs afetados pelo commit `efabc11`.
  - Apresentado o plano detalhado de expurgo de histórico antes de qualquer force push, garantindo a proteção do worktree e commits do S-17.
