# Execução — S-24 — 5ff72a9b-ce3a-4403-a7d4-0fed62bd18d9

- **Página do Notion e ID:** Card S-24 - Job de follow-up e nó reengajador (`26eb84e4-78ec-80f0-a152-eb04f2759e66`)
- **Agente / sessão:** Antigravity / Sessão `e28c99e3-09d4-4290-9b8f-e112f3438c3d`
- **Estado / última atividade UTC:** 2026-09-12T01:25:00Z — Checkpoint 2 (Implementação e validação concluídas, pronto para revisão e PRs)
- **Escopo e critérios de aceite consultados:**
  - Briefing `solar-ai-docs/execucoes/briefing-S-24-5ff72a9b-ce3a-4403-a7d4-0fed62bd18d9.md`.
  - Guia `WORKFLOW-AGENTES.md`.
  - Decisão de produto: Caminho (b) — Polling ao vivo na aba aberta + reidratação ao reabrir conversa.
  - 5 Verificáveis de aceite:
    1. Conversa inativa pelo limiar configurado (2 min para demo) recebe mensagem proativa de reengajamento e ela aparece na trilha ao reabrir.
    2. Mensagem cita dados concretos do perfil (região, preço máx) e não frase genérica ("oi, ainda aí?").
    3. 3ª varredura não gera 3ª tentativa (limite de 2 tentativas por lead/conversa).
    4. Conversa com desfecho `encerrar` ou encaminhada para corretor não recebe nada.
    5. Varredura com agente fora do ar não deixa mensagem pela metade no banco (atomicidade estrita).
  - Decisões deliberadas registradas:
    - Exigência de consentimento LGPD válido antes de reengajar (`Lead.TemConsentimento == true`).
    - Abertura de transação de banco de dados apenas após o retorno bem-sucedido do agente (chamada HTTP externa fora de transação).
    - Sentinela `[reengajar]` com guarda estrita: apenas o background service envia o header interno `X-Solar-Trigger: follow-up`. Mensagens de usuário enviadas via API pública com texto `"[reengajar]"` são tratadas estritamente como texto do lead, sem acionar o fluxo de reengajamento nem alterar contadores de follow-up.
- **Dependências por ID e evidências de conclusão:**
  - S-23 (Atribuição do corretor e encaminhamento): Concluído e integrado em develop.
  - S-20 (Grafo da Lia e nós de qualificação/apresentação): Concluído e integrado em develop.
- **Recursos reservados:**
  - Worktree S-24 em `worktrees/S-24` para `solar-ai`, `solar-ai-api`, `solar-ai-front` e `solar-ai-docs`.
  - Container PostgreSQL Compose isolado no projeto `solar-s24` com banco dedicado `solar_test`.
- **Repositórios / caminhos / branches / SHAs base:**
  - `solar-ai-api`: `worktrees/S-24/solar-ai-api` na branch `feature/S-24` (base `origin/develop` SHA `3ea0e62`)
  - `solar-ai`: `worktrees/S-24/solar-ai` na branch `feature/S-24` (base `origin/develop` SHA `53704ae`)
  - `solar-ai-front`: `worktrees/S-24/solar-ai-front` na branch `feature/S-24` (base `origin/develop` SHA `ec1394f`)
  - `solar-ai-docs`: `worktrees/S-24/solar-ai-docs` na branch `feature/S-24` (base `origin/develop` SHA `f214246`)
- **O que foi alterado:**
  - **solar-ai (Python 3.12.10):**
    - `prompts/reengajamento.md`: Template do prompt da Lia para reengajamento proativo contextualizado com o perfil acumulado e histórico.
    - `app/lia/prompts.py`: Função `reengajamento(perfil, historico)`.
    - `app/lia/grafo.py`: Adicionado nó `reengajar`, roteamento condicional inicial `_rotear_inicio`, parâmetro `reengajamento` em `responder`.
    - `app/main.py`: Extração do header `X-Solar-Trigger` no endpoint `POST /turn` sem alterar o schema Pydantic congelado do payload.
    - `tests/test_reengajamento.py`: 4 novos testes unitários cobrindo o prompt, nó do grafo, acionamento via header e proteção de sentinela.
  - **solar-ai-api (.NET 10 / SDK 10.0.400):**
    - `src/Solar.Api/Dominio/Conversa.cs`: Adicionadas propriedades `TentativasReengajamento` e `Desfecho`, métodos `RegistrarFollowUp` e `DefinirDesfecho`.
    - `src/Solar.Api/Persistencia/SolarDbContext.cs`: Mapeamento das colunas `tentativas_reengajamento` (default 0) e `desfecho` (varchar 30, nullable) em `conversas`.
    - `src/Solar.Api/Persistencia/Migrations/20260912001503_FollowUpTentativasEDesfecho.cs`: Migration EF Core com Up/Down simétricos e snapshot atualizado.
    - `src/Solar.Api/Agente/AgenteClient.cs`: Método `ReengajarAsync` que injeta `X-Solar-Trigger: follow-up`.
    - `src/Solar.Api/Conversas/ConversaRepositorio.cs`: Métodos `ObterIdsInativasParaFollowUpAsync` (filtrando corte de tempo, limite de tentativas, consentimento ativo, ausência de desfecho terminal e de encaminhamento) e `GravarFollowUpAsync` com transação atômica.
    - `src/Solar.Api/Conversas/ServicoDeReengajamento.cs`: Hosted service (`BackgroundService`) que orquestra a varredura periódica e chamadas seguras por conversa via `TravaDeConversas`.
    - `src/Solar.Api/Program.cs`: Registro de `ServicoDeReengajamento`.
    - `src/Solar.Api/appsettings.json`: Configuração de `FollowUp` (`IntervaloInatividade`, `IntervaloVarredura`, `LimiteTentativas`).
    - `tests/Solar.Api.Tests/FollowUpTeste.cs`: 7 testes unitários cobrindo os cenários e a guarda de sentinela.
    - `tests/Solar.Api.Tests/FollowUpPostgresTeste.cs`: 4 testes de integração contra PostgreSQL cobrindo ponta a ponta os 5 critérios de aceite.
  - **solar-ai-front (Angular 20.3.0 / TypeScript 5.9.2):**
    - `src/app/conversa/conversa-store.ts`: Polling a cada 3s via `iniciarPolling()`, `pararPolling()` e `verificarNovasMensagens()`. Reconstrução e reidratação da trilha com reset de sequência.
    - `src/app/conversa/conversa-store.spec.ts`: 4 novos testes unitários validando a chegada de follow-up via polling, desfecho e reabertura.
- **Validações: comando, resultado e ambiente (sem segredos):**
  - **solar-ai (Python):**
    - Comando: `pytest`
    - Diretório de execução (`Cwd`): `C:\Users\felip\Documents\FIAP\Fase_5_PRIVACIDADE_SEGURANCA_DE_DADOS\solar\worktrees\S-24\solar-ai`
    - Rootdir coletada: `C:\Users\felip\Documents\FIAP\Fase_5_PRIVACIDADE_SEGURANCA_DE_DADOS\solar\worktrees\S-24\solar-ai`
    - Configfile: `pytest.ini` (com `addopts = -m "not llm" --strict-markers`)
    - Resultado cheio: `226 passed, 1 skipped, 36 deselected in 1.95s` (0 falhas). O 1 skip é o teste que exige cache local de embeddings, e os 36 deselected são os testes marcados com `llm` para não consumir cota de produção durante testes locais determinísticos.
  - **solar-ai-front (Angular):**
    - Comando: `ng test --watch=false --browsers=ChromeHeadless`
    - Diretório de execução (`Cwd`): `C:\Users\felip\Documents\FIAP\Fase_5_PRIVACIDADE_SEGURANCA_DE_DADOS\solar\worktrees\S-24\solar-ai-front`
    - Resultado cheio: `TOTAL: 43 SUCCESS` (43 aprovados, 0 falhas, 0 ignorados).
  - **solar-ai-api (.NET):**
    - Ambiente: Docker Compose `solar-s24`, container `solar-s24-postgres-1`, banco dedicado `solar_test` no Postgres 16.
    - Comando: `dotnet test` (suíte COMPLETA, SEM filtro de exclusão).
    - Diretório de execução (`Cwd`): `C:\Users\felip\Documents\FIAP\Fase_5_PRIVACIDADE_SEGURANCA_DE_DADOS\solar\worktrees\S-24\solar-ai-api`
    - Resultado cheio: `Aprovado: 97, Com falha: 0, Ignorado: 0, Total: 97, Duração: 3 s`.
    - Todos os 8 testes de integração com Postgres (incluindo `FollowUpPostgresTeste` e `AgendaConcorrenciaPostgresTeste`) executados e aprovados.
- **Pendências ou bloqueios:** Nenhuma pendência técnica. Aguardando aprovação do Checkpoint 2 para criação dos PRs.
- **PRs e commits de entrega por repositório:**
  - (Aguardando liberação do Checkpoint 2 para abertura dos PRs de `feature/S-24` para `develop`).
- **Alterações propostas para ESTADO, arquitetura e vault:**
  - `ESTADO.md`: Registrar conclusão do nó reengajador no `solar-ai` e job de follow-up no `solar-ai-api` e polling no `solar-ai-front`.
  - `ARQUITETURA.md`: Documentar o header interno `X-Solar-Trigger: follow-up` e a máquina de estados de inatividade/reengajamento.
  - Vault `Solar Brain`: Adicionar nota conceitual sobre o sentinela com header de guarda e política de reengajamento com respeito estrito a consentimento LGPD.
- **Próximo passo exato:**
  - Reportar evidências a Claude Code via `maestri ask "Claude Code"`.
  - Após aprovação, commitar alterações pendentes no `solar-ai-docs`, abrir PRs de `feature/S-24` para `develop` em todos os repositórios afetados e atualizar status no board do Notion para `Revisão`.
- **Integração: responsável, SHAs em develop e validação conjunta:** A ser preenchido pelo integrador.
