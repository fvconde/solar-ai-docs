# Execução — S-23 — cf9a3306-ac5a-42c7-9a8c-ca9e7a736588

- **Página do Notion e ID:** Card S-23 · Roteador multiagente entre especialistas (`3c43478c-8945-8171-addb-d391fbb00bf4`) — [Link Notion](https://app.notion.com/p/S-23-Roteador-multiagente-entre-especialistas-3c43478c89458171addbd391fbb00bf4)
- **Agente / sessão:** Antigravity (Gemini 3.8 Flash High) / Execução `cf9a3306-ac5a-42c7-9a8c-ca9e7a736588`
- **Estado / última atividade UTC:** `Em revisão` / 2026-09-12T19:55:00Z
- **Escopo e critérios de aceite consultados:**
  - Guia de coordenação `WORKFLOW-AGENTES.md`.
  - Critério de aceite oficial do card no Notion:
    - O grafo tem um nó supervisor que roteia cada turno para `qualificador`, `consultor`, `agendador` ou `reengajador`.
    - O roteamento aparece no log INFO de cada turno contendo o GUID da conversa e o nó escolhido (`Supervisor roteou conversa <guid> para o no <especialista>`).
    - Verificável: quatro turnos de naturezas distintas produzem quatro rotas diferentes no log.
    - Custo em chamadas por turno NÃO sobe em relação ao estado anterior: 1 chamada no turno comum, 2 chamadas no turno que sugere imóveis.
    - Suíte grátis de testes do agente continua verde, incluindo os quatro desfechos do `-m llm -k handoff`.
  - Resolução e delimitação arquitetural:
    - **O nó supervisor é estritamente DETERMINÍSTICO e tem custo ZERO de LLM.** Não é um agente autônomo baseado em prompt/LLM, mas um supervisor estruturado como função pura determinística em Python.
    - Não se trata de renomeação cosmética de `_apos_pontuar`: o roteamento passa a ser explícito, centralizado na entrada do turno (`START -> supervisor`), cobrindo deterministicamente os quatro especialistas e emitindo log observável.
    - O grafo possui **TRÊS nós que chamam LLM** (`_responder`, `_reengajar` e `_apresentar`), enquanto `_qualificar`, `_agendar`, `_pontuar`, `_consultar` e `_supervisor` são funções puras determinísticas.
    - Contrato congelado do `POST /turn` (6 tipos, 37 campos) mantido 100% intacto.
- **Dependências por ID e evidências de conclusão:**
  - `S-15`: Integrado em develop (busca vetorial e nó apresentar).
  - `S-17`: Integrado em develop (slots de agenda via `TurnoRequest` e nó puro agendar).
  - `S-24`: Integrado em develop (nó `reengajar`, template `prompts/reengajamento.md` e header `X-Solar-Trigger: follow-up`).
- **Recursos reservados:**
  - Grafo da Lia: `app/lia/grafo.py` e nós de roteamento condicional.
  - Prompts compartilhados (inalterados): `prompts/turno.md`, `prompts/apresentacao.md`, `prompts/persona.md`, `prompts/reengajamento.md`.
  - Cota diária do Gemini: orçamento autorizado de 60 chamadas (4 baseline + 56 validação).
  - Registro próprio de execução em `solar-ai-docs/execucoes/S-23-cf9a3306-ac5a-42c7-9a8c-ca9e7a736588.md`.
  - Não tocados nem reservados: contrato congelado `POST /turn`, migrations do EF, DTOs .NET, store do front (pertencentes ao S-36 em paralelo) e Compose/portas da janela integrada.
- **Repositórios / caminhos / branches / SHAs base:**
  - `solar-ai`: `worktrees/S-23/solar-ai`, branch `feature/S-23`, SHA base `af389a6`
  - `solar-ai-docs`: `worktrees/S-23/solar-ai-docs`, branch `feature/S-23`, SHA base `ff4c137`
  - `solar-ai-api`: `worktrees/S-23/solar-ai-api` (HEAD destacado de apoio em `origin/develop`, não editado)
  - `solar-ai-front`: `worktrees/S-23/solar-ai-front` (HEAD destacado de apoio em `origin/develop`, não editado)
- **O que foi alterado:**
  - **solar-ai (Python 3.12.10):**
    - `app/lia/grafo.py`:
      - Implementado o nó supervisor determinístico `_supervisor` e função de decisão `_decidir_rota`.
      - Roteamento explícito para as quatro rotas especializadas:
        - `ROTA_REENGAJADOR` (`"reengajador"`): acionada quando `reengajamento=True`.
        - `ROTA_AGENDADOR` (`"agendador"`): acionada quando a requisição carrega horários em `requisicao.agenda`.
        - `ROTA_CONSULTOR` (`"consultor"`): acionada para consultas diretas a imóveis (`[consultar]` ou perfil sem lacunas essenciais com intenção explícita de catálogo/busca).
        - `ROTA_QUALIFICADOR` (`"qualificador"`): rota padrão para turnos de conversa e qualificação com lacunas em aberto.
      - Log INFO obrigatório emitido a cada turno: `"Supervisor roteou conversa %s para o no %s"`.
      - Topologia do grafo reestruturada: `START -> supervisor -> conditional_edges(_rotear_supervisor) -> {qualificador, agendador, consultor, reengajador}`.
      - Topologia preservada a jusante: `qualificador -> responder -> pontuar`, `agendador -> responder -> pontuar`, `reengajador -> pontuar`, `pontuar -> consultor | END`, `consultor -> apresentar | END`, `apresentar -> END`.
    - `tests/test_supervisor.py`:
      - Criação de 13 testes unitários e de integração de grafo offline (com dublê de LLM e mock de índice):
        - Topologia com nó supervisor e os 4 nós especialistas.
        - Comprovação de custo ZERO de LLM no supervisor (`test_supervisor_nao_gasta_chamada_llm`).
        - Roteamento individual para cada uma das quatro rotas.
        - Verificável literal do aceite: quatro turnos de naturezas diferentes gerando as quatro rotas distintas no log com seus respectivos GUIDs de conversa (`test_quatro_turnos_de_naturezas_diferentes_produzem_quatro_rotas_distintas_no_log`).
        - Garantia de invariância no consumo de chamadas por turno (`TestCustoDeChamadasNaoSobe`): 1 chamada para reengajamento, 1 chamada para agendamento, 1 chamada para qualificação comum, 1 chamada para consultor direto, 2 chamadas para qualificação que conclui essenciais e sugere imóveis.
  - **solar-ai-docs:**
    - `execucoes/S-23-cf9a3306-ac5a-42c7-9a8c-ca9e7a736588.md`: este registro de execução detalhado.
- **Validações: comando, resultado e ambiente (sem segredos):**
  - **1. Linha de base de LLM (executada antes de qualquer alteração):**
    - Comando: `.venv\Scripts\pytest -m llm -k handoff`
    - Resultado real: `4 passed, 273 deselected, 1 warning in 22.18s`
    - Casos validados:
      - `handoff-lead-vago-nao-vai-para-corretor` (PASSED)
      - `handoff-lead-sem-nenhum-dado-nao-vai-para-corretor` (PASSED)
      - `handoff-pedido-explicito-de-humano` (PASSED)
      - `handoff-investidor-completo-vai-ao-especialista` (PASSED)
    - Consumo: 4 chamadas de LLM.
  - **2. Suíte offline grátis (custo zero de LLM):**
    - Comando: `.venv\Scripts\pytest -m "not llm"`
    - Resultado real: `240 passed, 37 deselected, 16 warnings in 1.98s`
    - Contexto: 227 testes herdados da base integrada do S-24 + 13 novos testes criados em `test_supervisor.py` = 240 testes grátis 100% verdes.
  - **3. Teste do supervisor e verificáveis de log:**
    - Comando: `.venv\Scripts\pytest -s tests/test_supervisor.py -v`
    - Resultado real: `13 passed in 0.88s`
    - Comprovação das 4 rotas no log com GUID (`--log-cli-level=INFO`):
      ```text
      INFO  solar.lia:grafo.py:458 Supervisor roteou conversa 5243a3ee-e1e1-46e0-a0b0-6feb7ac560e3 para o no reengajador
      INFO  solar.lia:grafo.py:458 Supervisor roteou conversa 6e83b0d5-a4ab-4cd5-b03b-e834a1deb6c6 para o no agendador
      INFO  solar.lia:grafo.py:458 Supervisor roteou conversa 165654a1-bd8f-43f7-a1f2-82978d6977a6 para o no consultor
      INFO  solar.lia:grafo.py:458 Supervisor roteou conversa d4fce943-2cec-4424-98eb-bb41e8e0d2e5 para o no qualificador
      ```
  - **4. Validação live com LLM (Opção B - bateria conversacional completa):**
    - Justificativa da seleção: A Opção A cobriria apenas o texto estrito dos desfechos de handoff (4 chamadas), sendo insuficiente para aferir a integridade da nova entrada centralizada do grafo. A Opção B valida todos os 31 fluxos de geração conversacional (`test_agenda_live.py`, `test_extracao.py`, `test_mascaramento.py`, `test_reengajamento.py`), garantindo que nenhuma regressão foi introduzida nas rotas especializadas. A exclusão de `TestBuscaReal` (6 testes) preserva a cota de 100 req/min de embeddings (que consumiria 85 chamadas em lote) sem prejuízo técnico, uma vez que a busca vetorial em memória é puramente determinística e não foi alterada.
    - Comando: `.venv\Scripts\pytest -m llm -k "not TestBuscaReal"`
    - Resultado real: `31 passed, 246 deselected, 1 warning in 219.51s (0:03:39)`
    - Falhas: 0.
- **Disciplina e contabilidade de cota do Gemini:**
  - Orçamento total aprovado para o card: 60 chamadas (de 500 diárias).
  - Chamadas gastas na linha de base: 4 chamadas.
  - Chamadas gastas na validação live: 31 chamadas.
  - Total de chamadas consumidas pelo S-23: **35 chamadas**.
  - Saldo remanescente de segurança do orçamento: **25 chamadas**.
- **Pendências ou bloqueios:**
  - Nenhuma pendência técnica.
  - Merge em develop não executado (conforme regra estrita do projeto).
- **PRs e commits de entrega por repositório:**
  - `solar-ai`: Commit `8a6b8f5`, [PR #5](https://github.com/fvconde/solar-ai/pull/5) para `develop`
  - `solar-ai-docs`: Commit `2df068d`, [PR #8](https://github.com/fvconde/solar-ai-docs/pull/8) para `develop`
- **Alterações propostas para ESTADO, arquitetura e vault:**
  - `ESTADO.md`: Registrar a entrega do diferencial multiagente do card S-23 com o nó supervisor determinístico e a observabilidade centralizada de roteamento por conversa nos 4 nós especialistas.
  - `ARQUITETURA.md`: Atualizar a topologia da Lia destacando o nó supervisor na entrada (`START -> supervisor`) como função pura determinística, o log de roteamento estruturado e o fluxo condicional para `qualificador`, `consultor`, `agendador` e `reengajador`.
  - `Solar Brain`: Adicionar nota conceitual sobre a decisão de supervisor determinístico com custo zero de LLM, mantendo o teto de 1 chamada por turno comum e 2 por turno com vitrine.
- **Próximo passo exato:**
  - Integrador revisar os PRs de `solar-ai` e `solar-ai-docs` para `develop`, validar a suíte conjunta e consolidar `ESTADO.md` e `ARQUITETURA.md`.
- **Integração: responsável, SHAs em develop e validação conjunta:**
  - A ser preenchido pelo integrador.
